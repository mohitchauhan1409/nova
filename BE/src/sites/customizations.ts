import type { SiteProfile } from '../../../shared/types';
import { sageoxProfile } from './sageox';

export const customSiteProfiles: SiteProfile[] = [sageoxProfile];
export const siteStoreFilename = 'sites.sageox.json';

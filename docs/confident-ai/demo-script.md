# Northstar returns support demonstration

Draft final script, updated 22 September 2026. [live-verification.md](live-verification.md) distinguishes saved results from blocked and recovered attempts. This is not a completed recording. All company facts and messages are synthetic. Use only Vector / My first project (`cmubhm6yf0002o30tjtp072vo`).

## Supported scope and baseline

Reuse **Northstar Returns Adviser** (`cmubijzhk0005pb0tn3tnafcn`) and the existing Single-turn dataset (`cmubimef70007qs0tgjd1c7sq`). The prompt's saved second refinement, `ecd3bf0`, asks one missing detail at a time and keeps a 120-word limit. It is a real baseline for grouped follow-ups and a later 100-word revision.

Six unique goldens are now saved and independently verified. The second creation rehearsal added an unused standard jacket delivered **31 days** ago with a receipt and a **personalized** flask damaged **six days after delivery**. That saved result required multiple requests and one approved Save; it was not an uninterrupted clean pass. The final starts with **six** saved cases and creates two distinct boundary cases, ending with **eight** only after independent verification. Reuse matching cases and never delete earlier rehearsal rows to manufacture creation footage.

Existing-golden editing is excluded: Nova's approved visual pencil click and approved inline-cell double-click did not produce a verified editor. The final uses the already demonstrated New Golden creation form. A short follow-up revision changes the prompt's word limit, not a dataset row.

Content commits are available. Prompt branching and dataset version snapshots are paid-gated. Evaluation has not run and has no configured metric collection/AI connection. Keep Actual Output blank and other execution fields unset; invent no scores, traces, costs, refunds or completed return actions.

## Concise final operator sequence

1. From normal project Home, freshly launch Nova with one target tab and the panel closed; open its launcher. Ask: **“Rename our returns dataset to Northstar Returns Readiness and save it.”** Verify the same dataset's persisted alias before any clarification card. Reset only this alias to Coverage outside the take.
2. Ask: **“Now improve our returns prompt for unclear requests. Help me choose how it should ask follow-ups.”** Nova reads the existing one-question-at-a-time prompt and preserves the known policy, variable and tone.
3. Use **Help me choose** naturally. Answer: **“Collect the relevant missing details in one concise follow-up. Ask for delivery timing and receipt status together when needed, without repeating facts the customer gave. Keep the damage guidance and commit the change.”** Verify the saved content commit, full Preview and exactly one `{customer_message}` variable.
4. Ask: **“Add and save two new boundary cases: an unused standard jacket delivered exactly 30 days ago with a receipt, and a standard mug delivered exactly seven days ago and arrived damaged. Tag them window-boundary and damage-review respectively. Use our saved returns policy, reuse matching cases, and leave execution outputs empty.”** Inspect the actual existing inputs before creating. Preserve all six baseline cases; save exactly the two absent boundary cases.
5. Ask: **“Keep the prompt’s policy and grouped follow-ups, but shorten the response limit to 100 words and commit the change.”** Verify the new limit and unchanged policy/variable in the same saved prompt. This supplies a real targeted revision without the unreliable row editor.
6. Close with **“Which policy cases are covered now?”** and **“Show the saved prompt change.”** If useful, ask **“What still needs setup before we can evaluate this?”** Distinguish authored assets from unrun evaluation; do not start a trial or expose keys.

## Proposed final case targets

These are proposed authored assets, not claims that records already exist. Record actual IDs and exact saved wording after creation.

| Case | Proposed input | Tag | Expected Output requirements |
| --- | --- | --- | --- |
| Inclusive 30-day standard-return boundary | My unused standard jacket was delivered exactly 30 days ago and I have the receipt. Can I return it? | `window-boundary` | Apply the saved 30-day rule and receipt/unused conditions; explain eligibility and a suitable next step without claiming a refund, label or return has already been issued. |
| Inclusive 7-day damage-review boundary | My standard mug was delivered exactly seven days ago and arrived damaged. What should I do? | `damage-review` | Apply the saved within-7-days review rule; ask for order reference and damage description if needed; do not guarantee a refund or claim a completed action. |

Use “delivered exactly seven days ago and arrived damaged,” with the explicit tag request as a separate sentence. The tag-entry policy currently treats the bare word “after” in a request as a possible conditional restriction; the second rehearsal needed another request to recover from that false approval. This wording documents a limitation rather than claiming it was fixed.

The second rehearsal's 31-day jacket checks an outside-window outcome. Its personalized flask damaged at 6 days checks damage review separately from a change-of-mind exclusion; a personalized item's damage review is not an invented change-of-mind exception. Do not confuse those records with the two final boundary cases.

## Acceptance

Independently reopen the alias, both new goldens' full Inputs/Expected Outputs/tags/blank Actual Output, eight unique rows, and both final prompt commits. Preserve all prior rows and distinguish their exact inputs. A toast, action receipt or final chat message alone does not prove persistence. Keep all failed attempts and recoveries in the ledger.

Use trusted saved record links through ordinary navigation and verify the project and alias. The prompt index has omitted an existing record; dataset visual index opening remains unresolved. Keep Nova's actual work at normal speed. Original, silent and approved-tap edits, audiovisual QA and private backup remain pending until separately verified.

Frozen live runtime for this take: backend `94debcde941dcb30de7e3b89aebf26f963b109d9`, extension `b7e3172` (archive hash prefix `28e115`). Later source-guide or documentation commits do not change the already loaded backend identity. The latest source-guide corrections were not activated by this documentation update.

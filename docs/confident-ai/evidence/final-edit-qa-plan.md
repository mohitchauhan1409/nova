# Confident AI final edit and source evidence

The source is untouched native 3024×1776/30 fps, 16,922 frames /564.066667 seconds, with no audio. Identity and captured build/extension are recorded in final-source-identity.json. The complete 132 collector exports yield 42 real in-range receipts: 32 Nova and 10 operator, including launcher and focus before the first message. Every source receipt frame was extracted and visually inspected; no sounds are fabricated for Enter, typing, thinking, scrolling or replies.

All Nova execution and operator typing stay at 1×. The entire 128.001-second case task—including repeated column-selector verification—and its honest partial tag-verification answer remain. The operator's actual “Leave the columns as they are...” recovery is fully visible and unaccelerated. No splice or speed change implies an uninterrupted successful flow.

Only four visibly settled operator waits are shortened to 1 second each: 5–16s clean opening after 5s establishment;78–85s after 6.851s reading the renamed result;130–138s after 11.864s reading the first card;199–209s after 9.885s reading the saved prompt result. Native pause-panel samples confirm stable content without typing/action; no actual click falls in these spans. Source retained through 560s leaves 8.506s after the final Preview response. Output: 15,840 frames /528.000s. No crop, zoom or title cards.

Measured debugger-row mask: x10 / y174 / width2228, color#f9f8ff, no mask through 1526. Height 16 on frames 1527–1528, 56 on 1529, 88 on 1530, 112 from 1531. This covers the actual row including Cancel/X and leaves the boundary shadow/product below intact. These are current-source measurements, independently corroborated; AgentMail timing was not reused.

Source-wide 5s contacts, all 42 native receipt frames and native debugger-entry/idle frames show normal product content and the actual Nova arrow/solid light label/matching ring at action targets. No additional system/tool pointer was observed in those samples. Ordinary carets stay visible. This is bounded source inspection, not exhaustive every-pixel certification.

The approved frame editor runs with empty clicks and 2 ffmpeg threads; its required secondary output is temporary/discarded. The unchanged AgentMail-approved mechanical renderer uses 48 kHz stereo, seed 2071, gain 1.6 and cap 0.30. Final media are exactly raw/silent/clicks. Post-render checks cover full canvas/frame count, same encoded video, all mapped cues, decoded onset/correlation/quiet/clipping, native mask boundaries and saved ending. Local export and independent numerical/bounded visual acceptance now pass; see final-qa-report.md and independent-final-acceptance.md. Remote recovery now passes for all three complete files; see [remote backup verification](remote-backup-verification.json). Root independently confirmed saved tags, 8 rows and prompt commit 5fb11cb; detailed live coverage is maintained by the docs owner.

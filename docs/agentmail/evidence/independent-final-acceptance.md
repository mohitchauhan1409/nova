# Independent AgentMail final QA acceptance

**PASS for the bounded independent review. No blocking export defect found; the coordinator can proceed with backup.** This decision combines a fresh numerical/audio check with newly decoded opening, actual-cursor, debugger-animation and ending frames. It is not a claim that every pixel of the six-minute export was watched independently.

- Both delivered MP4s are native 3024×1776, 30 fps, 10,857 frames / 361.900 s. Silent has no audio; clicks has 48 kHz stereo AAC. Their complete encoded video stream hashes match: `b628e691f5ef95cac9ee1cc2ee65dd064244f3618da33a212e969f3095d5936c`.
- The original remains 11,602 frames / 386.733333 s and SHA-256 `20640dd4f09cc5fc55d7119d91393b68bd523efdc7c0e3e355972e3c1e59a9d7`.
- Independently deduplicating the available 38 `sessions-*.json` exports recovered exactly 37 actual in-range receipts: 25 Nova and 12 operator. Every receipt agrees with the shipped cue and its nearest source-frame/EDL mapping. None occurs within an accelerated operator-idle interval. Every non-idle segment preserves its original frame count. The numerical evidence is in `independent-evidence.json`. The worker's prose says 39 source exports; this reviewer found 38 matching session export files, but the complete click set agrees and this does not block the media.
- A fresh audio-only decode independently matches all 37 approved taps: minimum correlation 0.999652, maximum onset difference 0.041667 ms, decoded peak 0.217083 (no clipping), and exactly zero peak outside guarded click windows. Audio duration is exactly 361.9 s. See `independent-audio.json`; no existing worker audio-verification result was used to produce these numbers.
- `opening.png` at output 4.000 s shows one AgentMail tab, the matching dark Nova welcome panel before task execution, full native canvas and no debugging-row mask or operator pointer. No right padding or distorted page/panel geometry is visible.
- `source-cursor.png` and `final-cursor.png`, independently decoded at 37.500 s before the first idle reduction, show the same single white Nova arrow and opaque light action label at the real display-name field. The product and panel match apart from the permitted debugger-row mask. No extra system/tool pointer is visible in these samples.
- `mask-entry-independent.png` compares newly decoded source and final frames 627–636 across the full width. Frames 627–629 have no premature mask. Entry frames 630/631/632/633 follow heights 34/74/98/112; debugger text, Cancel and X all disappear while the product begins at the same boundary beneath. Interior RGB is [249,248,255], with no surviving dark button/text pixels in the tested rectangle. The product strip immediately below differs from source by only 0.122–0.165 mean intensity units out of 255, consistent with encoding rather than cover. See `mask-boundary-check.json`.
- `ending.png` is the final output frame. It shows one open Edit Draft with recipient `orders@customer.example`, subject `Your oak desk order update`, order CF-1048, dispatch **October 5, 2026**, tracking-after-dispatch wording and Cedar & Finch Care sign-off. The draft remains on Drafts with two total rows; Nova's final response correctly says it is already open for review. The earlier summary states purpose Furniture order support, response target 1 business day, and unsent/unscheduled status. There is no fabricated final click cue for that already-open response.

Scope was intentionally restricted to fast targeted offline QA at the coordinator's request. No UI, runtime, build, commits, source changes, heavy full-duration video decode or additional render was performed. The raw and both primary exports remain untouched.

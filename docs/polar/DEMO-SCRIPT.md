# Recorded Polar script

The final take uses the signed-in Vector workspace and real synthetic configuration. The opening performs a useful change before any question card. No confirmation card appears in the final take.

1. **I can't find where to turn on rollover for our starter credits. Can you do it?**
   Nova finds Workflow Starter Credits and enables rollover, preserving Hidden, Workflow Executions and 1,000 units.
2. **Help me set up Workflow Units for successful AI workflows.**
   The Configure Workflow Units card receives Event name `vector.workflow.completed`, Success condition `status = success`, Numeric property `compute_units`, and Display unit `Scalar`. Aggregation is left unanswered. Select **Help me choose**. Nova retains all four answers, explains Count versus Sum, and asks only the remaining choice. Select **Sum compute_units**, then Continue. Nova saves and reopens the new meter with separate AND condition groups.
3. **Give our team plan 5,000 units using that meter.**
   Nova checks existing benefits, then asks which benefit and whether to roll over. For the first question, select Something else and type **Create a new benefit called Vector Team Credits.** Select **No rollover**, then Continue. Nova creates a hidden, unattached Meter Credits benefit using the exact Workflow Units meter and reopens it.
4. **Make that 7,500 units and keep everything else.**
   Nova updates the same benefit and verifies its saved detail. The exact meter, Hidden visibility and no-rollover setting remain unchanged.
5. **What did we save?**
6. **Is any real usage being billed yet?**
7. **Show me Home.**

All operator prompts use ordinary punctuation, with no em dashes. Typing is progressively visible at its recorded speed. The meter and benefit are created on camera; there is no hidden operator completion.

The benefit creation includes a short detour through unlabeled controls before Nova opens the correct drawer. That recovery remains at normal speed in the final video. Its intermediate “Verified” badges mean a page change was observed; the completed outcomes are independently checked by reopening exact saved objects.

Meters aggregate usage; they do not by themselves charge money. The aggregation advice uses the shorthand “charges units”; the closing exchange clarifies that this configuration has no product attachment or benefit grants. No usage events, transactions, customer grants, purchases or live product publication were performed.

Before another take, preserve these results. Use distinct named resources or explicitly revise the owned configuration; do not silently pre-complete the on-camera creation tasks. To repeat the opening, reset only the owned Workflow Starter Credits rollover to off and verify its saved state.

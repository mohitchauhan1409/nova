# Polar validation

The final uninterrupted take and both edited exports are complete. The opening acts
before asking questions; the second request introduces grouped cards. The final
session has 38 verified action steps and no approval events. Remote media recovery
is documented separately in BACKUP.md. Fixture results are not live website proof.

## Access and scope

The requested Chrome profile and signed-in Vector workspace were verified. The
original Computer connection failed with empty accessibility trees and
`noWindowsAvailable`; restarting the stalled Computer helper restored both AX and
coordinate input. Work continued in the original account. No alternate account,
website API, customer grant, usage ingestion, checkout or payment was used.

The private repository's `polar-nova` branch began at main
`2ddbe3cd8a68707985506745101728a0f6ca188c`. Unrelated `docs/roark/` work is preserved.
Baseline objects and exact IDs are in SYNTHETIC-MANIFEST.json. Empty analytics are
left honest. Product creation was excluded because Private products remain
purchasable by direct link and the observed editor offered no draft save.

## Shared fixes and regression checks

Reusable fixes were committed on main, pushed, then merged into polar-nova:

- `6c3ce0f`: distinguish ordinary form controls from submissions.
- `b414b05`: keep requested unattached record edits fluid; preserve sensitive
  boundaries; wait for staged form fields before abandoning an editor.
- `5aa8e74`: reobserve after slow planning and reject stale targets before input.
- `19c22d2`: recognize save buttons naming the current editor.
- `2469b91`: honor explicit contextual save requests in an identified ordinary
  editor without requiring the user to repeat its noun.
- `499dd22`: distinguish incidental billing help from actual billing fields and
  account editors; the requested named-record and sensitive-control checks remain.
- `fd8c033`: expose observed page appearance to hostname-scoped companion themes.
- `5c427ff`: resolve nested scroll containers and recover unchanged scrolls with visual inspection.
- `3dd2526`: preserve hydrated editor fields before a stale dismissal and name uniquely labelled custom toggles.
- `b68a7be`: ground requested ordinary edits in visible descriptions in the same form when a custom control has no accessible name.

The latest policy update passed 85 focused checks and TypeScript.
Latest full regression result: 20 files, 245 tests passed. TypeScript and the
recording-mode production build passed. Panel fixtures cover light/dark cards,
320/400/480px widths, timeline, Chat default and other-host isolation. Cursor
fixtures cover normal, hidden-capture and visible-capture modes with one neutral
accent. Live preflight confirmed a single anchored Nova action cursor and absent
system pointer. The latest affected-check capture/export also passed with a clean opening, neutral panel/cursor and progressively typed message. Its five actual tap receipts decoded with 0 ms onset error and minimum correlation 0.99965.

## Live workflow evidence

Failed runs are retained in local trace reports and are not counted as passes.

- Initial grouped meter questions retained event, status, numeric property and
  unit through Help me choose, then asked only for Count versus Sum. Execution
  stopped at an unnecessary Add condition group confirmation; fixed on main.
- Early action-first opening reached the exact starter benefit in four actions.
  First action began about 5.6 seconds after submission. Partially hydrated fields
  and false Cancel/Update approvals prevented a clean pass; fixed on main.
- Starter rollover save/reopen succeeded in 25.0 seconds after a fully specified
  resume. Reset to off/Home succeeded in 28.2 seconds. Both recovered from a stale
  Cancel after save; the freshness guard was added afterward. These do not prove
  a clean fresh opening on the latest build.
- Compute Units create/save/reopen succeeded in 101.3 seconds with 17 verified
  actions and no confirmation. First action began after 4.3 seconds. Independent
  reopened AX confirmed Name=vector.workflow.completed AND Metadata status=success,
  Sum of compute_units, Scalar, and the exact saved ID. This was the first meter
  pass; the later staging-filter variation supplied the second clean pass.

- Compute Allowance create/save/reopen succeeded in 94.0 seconds, with first
  action after 3.6 seconds. Fourteen actions verified. Nova recovered after two
  repeated Benefits navigations and created one exact resource. Independent AX
  confirmed Hidden, Compute Units, 5,000 units and rollover off in the reopened
  editor, with no products or grants on the saved detail.

- The first allowance revision was blocked before input because truncated help
  text included the word billing. This false confirmation was cancelled, fixed
  on main, and is excluded from successful rehearsal counts.

- Corrected allowance revision saved and reopened the same ID in 20.2 seconds,
  with no confirmation and three verified actions. Independent AX confirmed
  7,500 units and preserved meter, hidden visibility and rollover off.

- A short remembered correction from 7,500 to 6,000 passed in 20.5 seconds,
  preserving all other benefit fields. Stop halted planning before input; the
  subsequent Continue request resumed the exact meter task.
- The first extra-environment meter variation exposed two additional false form
  reviews (named Update Meter and a contextual save request). Both were cancelled
  and fixed. The saved production filter was independently verified, and Nova
  verified it visually after longer-editor scrolling. These interrupted repairs
  are excluded from clean rehearsal counts.

- The development-filter variation saved correctly, but verification repeated ineffective nested scrolls; excluded as a clean pass. The shared scroll resolver and unchanged-scroll screenshot recovery address that cause.
- Two revised-opening preflights reached the correct rollover toggle but unnecessarily reviewed Update. The second already waited correctly for hydrated fields. Polar renders rollover as paragraph text beside an unnamed checkbox; the same-form grounding correction fixes the missed intent match. Neither failed take is final media.

- Revised natural opening passed on main b68a7be: first action 6.55 seconds, saved result 51.98 seconds, six verified actions, no question or approval card. Independent reopened AX confirmed Hidden, Workflow Executions, 1,000 units and rollover enabled.
- The reverse variation saved rollover off and returned Home in 32.73 seconds, with three verified actions and no confirmation. Saved detail confirmed Balance resets; Home retained honest zero revenue/orders.

- The second clean meter rehearsal changed development to staging in 97.09 seconds (first action 4.38 seconds), with eight verified actions and no approval. Nova scrolled the real editor and used 80% zoom for the longer form; independent reopened AX confirmed all three AND groups, Sum compute_units and Scalar. Browser zoom is reset before the final take.

All elapsed times above are from Nova's source timestamps, not edited video.

## Audio reference

The approved Bolna edit-v4 reference was fetched from its remote LFS backup.
SHA-256: c656ad78874030216801528e0d6e38481bea73cd0b0c6e73c7e98c4154dc5347.
The reusable renderer functions are AST-identical to the approved renderer.
Reconstruction against the decoded approved artifact gave minimum correlation
0.9994650195514574 with zero off-cue peak. Details: evidence/tap-reference.json.
The final Polar export has 62 evidenced taps: 26 operator and 36 Nova clicks.
Decoded maximum onset error is 0.0417 ms (well inside one 30 fps frame), minimum
tap correlation is 0.9994684, and off-cue peak is zero. Peak is -13.05 dBFS. The
silent and click-only exports have identical encoded video streams. See
evidence/final-sound.json and evidence/final-decoded.json.

## Final take and measured timing

The final launch used the signed-in Vector account, Home, one target tab, Nova
closed with its launcher visible, and no debugger row. The exact prompts and card
answers are in DEMO-SCRIPT.md. All times below use original session timestamps,
not the compressed edit.

| Request | Measured result |
| --- | --- |
| Turn on starter credit rollover | First action 4.044 s; saved result 40.278 s; six verified actions; no question card |
| Set up Workflow Units | Grouped card 10.377 s; Help me choose response 7.239 s; saved and reopened 108.339 s after the final answer |
| Give the team plan 5,000 units | Missing benefit/rollover card 30.454 s; saved and reopened 119.616 s after the answers |
| Revise to 7,500 units | 22.165 s; same benefit identity and other fields preserved |
| What did we save? | 5.775 s |
| Is any real usage being billed yet? | 5.208 s |
| Show me Home. | 10.928 s |

The second request retained the event, success filter, numeric property and unit
through Help me choose, asking only for the unresolved aggregation. Nova created
Workflow Units, then the connected Vector Team Credits benefit, then revised the
same benefit. Independently reopened UI confirmed the saved meter filters, Sum
of compute_units, Scalar unit, exact meter relationship, 7,500 credits, Hidden
visibility and rollover off. No products or grants were attached. All six saved
synthetic resources and exact identities are in SYNTHETIC-MANIFEST.json.

The meter and benefit configuration paths each have multiple successful live
runs, including targeted revisions. Starter rollover passed both on and off
before the take. Stop and resume were exercised. Guide verification flags now
cover these documented configurations and review tasks only.

## Final export checks

- Raw: 10:55.733, 19,672 frames, 3024×1714, 30 fps, no audio. Its source hash is unchanged.
- Silent and click-only edits: 9:02.833, 16,285 frames, 2744×1714, 30 fps.
- Only actual right-side black capture padding was cropped. No browser content was cropped.
- Eighteen stationary operator pauses account for 99.9 seconds removed by compression; 13 seconds of unused ending were trimmed. All Nova execution/recovery, interaction motion and nine progressive typing intervals remain at source speed.
- Only the complete debugging row is covered, including its four-frame entrance and close icon. No privacy covers were applied.
- All 62 decoded click frames were visually inspected, along with opening, progressive typing, debugger entrance and final readable ending. The single neutral Nova cursor is anchored to its real actions; native capture excludes the physical and operator cursor. Normal text carets remain visible.
- Final decoded audio and unchanged encoded video checks passed. There is no microphone, keyboard, music or other added audio.

Evidence: final-capture.json, final-session.json, final-edit.json, final-cues.json,
final-video.json, final-sound.json, final-decoded.json and final-audit.json under
evidence/. Useful text traces are retained; failed takes and temporary visual
artifacts are removed after backup verification.

## Practical limits

This demonstrates saved configuration in an account with unfinished onboarding,
not event ingestion, a live product, credit grants or billing. Honest zero metrics
remain visible. Nova took a visible detour through notification and sorting
controls while locating an unlabeled benefit add button; its recovery remains in
the final video at normal speed. One tiny inactive timeline status dot retains
the shared muted lavender color; the action cursor and principal panel controls
use the Polar neutral theme. The aggregation advice uses “charges” as shorthand;
a meter measures usage and does not itself start billing, as the closing answer
clarifies. Coverage is specific to the documented live flows, not exhaustive.
